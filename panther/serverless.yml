service: panther

provider:
  name: aws
  runtime: nodejs12.x

functions:
  panther:
    handler: handler.panther
    events:
      - http:
          path: panther
          method: get
          request:
            parameters:
              querystring:
                host: true
                path: true
    timeout: 30
